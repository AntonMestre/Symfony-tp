<!DOCTYPE html>
<html>
    <head>
        <!-- Required meta tags -->
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
        <!-- Affichage titre de page  -->
        <title>{% block title %}{% endblock %}</title>
    </head>
    <body>
      <!-- _________________________________________________________________  -->
      <!-- BARRE DE NAVIGUATION  -->
      <div class="container">
        <div class="row">
          <div class="col-md-2" >

            <!-- TITRE DE PROSTAGE  -->
            <h1 style="padding-top:10px;"><span>Pro</span><span style="color:#0068FF;">Stage</span></h1>

          </div>
          <div class="col-md">
            <ul class="nav justify-content-end" style="padding-top:15px;">

              <!-- LIENS DU MENU  -->
              <li class="nav-item">
                <a class="nav-link" aria-current="page" href="{{ path('index') }}" style="color: inherit;"> <i class="bi bi-house-door"></i> Accueil</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{ path('entreprise') }}" style="color: inherit;"> <i class="bi bi-building"></i> Trier par entreprise</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{ path('formation') }}" style="color: inherit;"> <i class="bi bi-book"></i> Trier par formation</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{ path('ajoutEntreprise') }}" style="color: inherit;"> <i class="bi bi-person-plus-fill"></i> Ajouter une entreprise</a>
              </li>

            </ul>
          </div>
        </div>
      </div>

      <!-- _________________________________________________________________  -->
      <!-- CONTENUE DE LA PAGE  -->

      <div class="container">
        <div class="row">
          <div class="col-md-12">
            <div class="d-flex justify-content-center">

            <!-- MESSAGE DE DESCRIPTION DE PROSTAGE  -->
            <p style="padding-top:50px;"> Pro<span style="color:#0068FF;">Stage</span> vous permet de voir les offres de stages disponible dans le Pays Basques.</p>

            </div>
          </div>
        </div>
      </div>
        <br/>
        <div class="container">
          <div class="row">
            <div class="col-md-12">
              <!-- CONTENUE A AJOUTER  -->
              {% block body %}{% endblock %}
            </div>
          </div>
        </div>
        {% block javascripts %}{% endblock %}

        <!-- Inclusion de bootstrap -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js" integrity="sha384-ygbV9kiqUc6oa4msXn9868pTtWMgiQaeYH7/t7LECLbyPA2x65Kgf80OJFdroafW" crossorigin="anonymous"></script>
    </body>
</html>
